server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/cstefanakis/spring-boot-4-microservices.git
          search-paths: configurations

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
